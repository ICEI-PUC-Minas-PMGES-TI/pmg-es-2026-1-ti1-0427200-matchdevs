const API_BASE_URL = "http://localhost:3000";

const API = {
  usuarios: `${API_BASE_URL}/usuarios`,
  empresas: `${API_BASE_URL}/empresas`,
  vagas: `${API_BASE_URL}/vagas`,
  candidatos: `${API_BASE_URL}/candidatos`
};